
val list = List(1, 4, 2, 2, 1, 5)
val set = Set(1, 4, 2, 2, 1, 5)
val map = Map(1 -> "a", 2 -> "b")

0 :: list
list.prepended(0)

set + 0 + 100
map + (3 -> "c") + (1 -> "dddd")

list
set
map


List(map, map, map)

